{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the orchestrator window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the orchestrator window.",
  "windows": ["main", "service-topology-window"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
